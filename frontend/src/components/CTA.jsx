import React from 'react';

const CTA = () => {
  return (
    <section className="cta container">
      <h2 className="section-title">Join the Fun!</h2>
      <p className="section-sub">Register now to get exclusive updates.</p>
      <button className="btn btn-primary btn-lg">Register Now</button>
    </section>
  );
};

export default CTA;
